spring:
  datasource:
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/fornecedor?useTimezone=true&serverTimezone=UTC
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
